"""
Data cleaning with Polars. Placeholder for transform steps (e.g. dedup, normalize).
"""
import polars as pl

def transform(df: pl.DataFrame) -> pl.DataFrame:
    """Apply cleaning/transform steps. Currently a pass-through."""
    return df
